I have created the project 1 which was about creating a rule based AI chatbot that masters control flow and logic.
Instead of using a messy if-elif ladder (which has a slow O(n) linear lookup time).
This implementation uses a Python Dictionary to achieve an efficient, constant O(1) lookup time using the .get() method.
